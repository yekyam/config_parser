# Config_parser - It's in the name

A simple config_parser, so I don't have to write this for every project.

Reads a config file in the form of an .ini file

Example:
```
foreground = RED
background = BLUE
```